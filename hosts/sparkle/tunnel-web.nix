# The port sparkle serves its misc file server on over the WireGuard tunnel, which sparxie proxies at pub.bunny.enterprises. Read by the listener, the firewall opening on wg0, and sparxie's upstream.
{
  port = 9000;
}
